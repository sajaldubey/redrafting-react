import helpful from "./helpers";  
// it cant be from "helpers" as it would refer to modules instead
// default imports are used without curly braces, other imports from the
// same file are done inside curly braces and are name specific
helpful();